# CodeThrive Infotech — Website (codethrive-website)

**Stack:** React + Vite + Tailwind CSS + React Router + EmailJS (frontend)

This project is a multi-page React site ready to deploy to Vercel. It includes an EmailJS-ready contact form, SEO meta tags, a favicon placeholder, and social placeholders.

---

## Quick start (local)

1. Create project folder & paste files (this repo)
2. Open terminal in project root
3. Install dependencies:

```bash
npm install
```
